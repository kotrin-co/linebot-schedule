<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

<h1>予約管理画面</h1>
<% reservationsData.forEach((item)=>{ %>
    <li><%= item.id %>:<%= item.name %>:<%= item.starttime %>:<%= item.endtime %></li>
<% }); %>

</body>
</html>